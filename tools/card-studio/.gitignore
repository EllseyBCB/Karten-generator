# Abhängigkeiten
node_modules/

# Deine Basisgrafiken bleiben lokal (nur Ordner wird versioniert)
input/*.png
# Ausnahme: das Zahlen-Sheet darf ins Repo (damit es hochgeladen werden kann)
!input/glyph_sheet.png
input/glyphs/*.png

# Generierte Karten
output/*.png
